# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=cwh_download

# JWT 配置
# 生成方法: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_here_please_change_this_to_a_random_64_byte_hex_string
JWT_EXPIRES_IN=7d

# 服务器配置
PORT=3000
NODE_ENV=development

# 文件上传配置
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=104857600

# CORS 配置（可选，前后端分离时使用）
CORS_ORIGIN=http://localhost:5173

# 存储配额（字节）
DEFAULT_STORAGE_QUOTA=3221225472

# 访客会话过期时间（毫秒）
VISITOR_SESSION_TIMEOUT=300000
